
import React from "react";
import MessageInputSettings from "../msgSettings/msgInputSetting";

const SettingsPanel: React.FC = () => {
    return (
        <div className="sidebar">
            <MessageInputSettings />
        </div>
    );
};

export default SettingsPanel;
