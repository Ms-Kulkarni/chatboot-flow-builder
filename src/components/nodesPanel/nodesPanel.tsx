
import MessageNode from "../msgNode/msgNode";

const NodesPanel: React.FC = () => {
    return (
        <div className="sidebar">
            <MessageNode />
        </div>
    );
};

export default NodesPanel;
